rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /awsStarBattle/ranking {
      allow read, write: if true;
    }
    match /users/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
